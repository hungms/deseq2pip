% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsea_functions.R
\name{read_gsea_tsv_list}
\alias{read_gsea_tsv_list}
\title{Read GSEA Results from TSV Files}
\usage{
read_gsea_tsv_list(
  data_dir = getwd(),
  collection = c("HALLMARK", "GOBP", "KEGG", "REACTOME", "BIOCARTA", "TFT"),
  merge = T
)
}
\arguments{
\item{data_dir}{Parent directory containing comparison subdirectories. Default is current working directory}

\item{collection}{Vector of gene set collections to load. Default includes HALLMARK, GOBP, KEGG, REACTOME, BIOCARTA, and TFT}

\item{merge}{Logical. If TRUE, returns a single merged data frame. If FALSE, returns a list of data frames}
}
\value{
Either a merged data frame or a list of data frames containing GSEA results
}
\description{
This function reads GSEA results from TSV files for multiple comparisons and collections.
}
\examples{
# Read and merge all results
merged_gsea <- read_gsea_tsv_list("results", merge = TRUE)

# Read specific collections as a list
gsea_list <- read_gsea_tsv_list("results", collection = c("HALLMARK", "KEGG"), merge = FALSE)
}
