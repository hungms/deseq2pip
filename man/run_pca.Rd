% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02_dist.R
\name{run_pca}
\alias{run_pca}
\title{Run Principal Component Analysis}
\usage{
run_pca(
  vsd,
  var,
  pals = NULL,
  size = 4,
  save_prefix = NULL,
  save_data = TRUE,
  save_plot = TRUE,
  save_dir = getwd()
)
}
\arguments{
\item{vsd}{A DESeq2 object containing the normalized gene expression data}

\item{var}{Column names in colData(vsd) to group by.}

\item{pals}{Vector of colors to use for groups. If NULL, uses default ggplot2 colors. Default is NULL}

\item{size}{Size of points in the PCA plot. Default is 4}

\item{save_prefix}{Prefix for the save file. Default is NULL}

\item{save_data}{Logical. If TRUE, saves PCA results to TSV. Default is TRUE}

\item{save_plot}{Logical. If TRUE, saves the PCA plot to PDF. Default is TRUE}

\item{save_dir}{Directory to save the results. Default is the current working directory}

\item{shape}{Column name in colData(vsd) to use for shape in the PCA plot. Default is NULL}
}
\value{
A ggplot object showing the PCA plot
}
\description{
This function performs PCA on the variance-stabilized transformed data and generates a PCA plot.
It can visualize sample relationships and identify potential batch effects or outliers.
}
\examples{
\dontrun{
# Basic PCA plot
p <- run_pca(vsd)

# PCA plot with shape and save results
p <- run_pca(vsd, shape = "Batch", save_data = TRUE)

# PCA plot with custom colors
p <- run_pca(vsd, pals = c("red", "blue", "green"))
}
}
