% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsea_functions.R
\name{run_gsea}
\alias{run_gsea}
\title{Run Gene Set Enrichment Analysis}
\usage{
run_gsea(
  res,
  org = "human",
  group_by = "Group1",
  custom_msigdb = NULL,
  order = "rank",
  save_data = T,
  save_dir = getwd()
)
}
\arguments{
\item{res}{Differential expression result data frame from run_diffexp()}

\item{org}{The organism to use, either "human" or "mouse". Default is "human"}

\item{group_by}{Column name in colData(dds) to use for grouping. Default is "Group1"}

\item{custom_msigdb}{Path to a custom gene set database in TSV format. Default is NULL}

\item{order}{Column name to use for ranking genes. Default is "rank"}

\item{save_data}{Logical. If TRUE, saves results to TSV and RDS files. Default is TRUE}

\item{save_dir}{Directory to save the results. Default is current working directory}
}
\value{
A list of GSEA results for each gene set collection, with each element containing:
- gene set name
- normalized enrichment score (NES)
- p-value and adjusted p-value
- leading edge genes
- collection name
- comparison name
}
\description{
This function performs Gene Set Enrichment Analysis (GSEA) using clusterProfiler
to identify enriched gene sets in the differential expression results. It can use
either pre-defined MSigDB gene sets or custom gene sets.
}
\examples{
\dontrun{
# Run GSEA with default MSigDB gene sets
gsea_results <- run_gsea(res)

# Run GSEA with custom gene sets
gsea_results <- run_gsea(res, custom_msigdb = "path/to/custom_sets.tsv")

# Run GSEA for mouse data
gsea_results <- run_gsea(res, org = "mouse")
}
}
