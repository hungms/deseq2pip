% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_functions.R
\name{plot_gene_exprs}
\alias{plot_gene_exprs}
\title{Plot Gene Expression}
\usage{
plot_gene_exprs(
  dds,
  res = NULL,
  group_by = "Group1",
  genes,
  plot_name,
  cols = NULL,
  save_plot = T,
  save_dir = getwd()
)
}
\arguments{
\item{dds}{DESeq2 object containing the expression data}

\item{res}{Optional differential expression results data frame}

\item{group_by}{Column name in colData(dds) to group by. Default is "Group1"}

\item{genes}{Vector of gene names to plot}

\item{plot_name}{Name for the output plot}

\item{cols}{Vector of colors to use for groups. If NULL, uses default ggplot2 colors. Default is NULL}

\item{save_plot}{Logical. If TRUE, saves the plot to PDF. Default is TRUE}

\item{save_dir}{Directory to save the plot. Default is current working directory}
}
\value{
A ggplot object showing gene expression boxplots
}
\description{
This function creates boxplots showing the expression levels of selected genes across groups.
It can optionally add statistical significance indicators between groups.
}
\examples{
\dontrun{
# Basic expression plot
p <- plot_gene_exprs(dds, genes = c("GENE1", "GENE2"), plot_name = "my_genes")

# Expression plot with statistical significance
p <- plot_gene_exprs(dds, res = res, genes = c("GENE1", "GENE2"), plot_name = "my_genes")

# Expression plot with custom colors
p <- plot_gene_exprs(dds, genes = c("GENE1", "GENE2"), plot_name = "my_genes", cols = c("red", "blue", "green"))
}
}
