% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_functions.R
\name{plot_atac_annot}
\alias{plot_atac_annot}
\title{Plot ATAC-seq Peak Annotations}
\usage{
plot_atac_annot(
  dds,
  res,
  p.thresh = 0.05,
  fc.thresh = 0.5,
  save_dir = getwd(),
  save_plot = TRUE
)
}
\arguments{
\item{dds}{DESeq2 object containing the ATAC-seq peaks data}

\item{res}{Differential expression results data frame from run_diffexp()}

\item{p.thresh}{Adjusted p-value threshold for significance. Default is 0.05}

\item{fc.thresh}{Log2 fold change threshold for significance (absolute value). Default is 0.5}

\item{save_dir}{Directory to save the plot. Default is current working directory}

\item{save_plot}{Logical. If TRUE, saves the plots to PDF. Default is TRUE}
}
\value{
A list of ggplot objects showing the pie charts
}
\description{
This function analyzes differentially expressed ATAC-seq peaks and creates pie charts
of their genomic annotations. It filters peaks by adjusted p-value and log2 fold change,
cleans up annotation labels, and generates three pie charts: one for all DE peaks,
one for upregulated peaks, and one for downregulated peaks.
}
\examples{
\dontrun{
# Generate annotation pie charts for ATAC-seq peaks
plots <- plot_atac_annot(dds, res)

# Use custom thresholds
plots <- plot_atac_annot(dds, res, p.thresh = 0.01, fc.thresh = 1)
}
}
