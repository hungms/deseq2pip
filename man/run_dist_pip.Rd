% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02_dist.R
\name{run_dist_pip}
\alias{run_dist_pip}
\title{Run Sample Distance Pipeline
This function runs the distance analysis portion of the RNA-seq analysis pipeline,
including generating PCA and distance plots, and saving distance data.}
\usage{
run_dist_pip(
  dds,
  var,
  batch = NULL,
  pals = NULL,
  save_dir = getwd(),
  save_dir_name = "qc_results"
)
}
\arguments{
\item{dds}{DESeq2 object containing the expression data}

\item{var}{Column name in colData(dds) to use for grouping. Default is "Group1"}

\item{pals}{Vector of colors to use for groups. If NULL, uses default ggplot2 colors. Default is NULL}

\item{save_dir}{Directory where all output files will be saved. Default is current working directory}

\item{save_dir_name}{Name of the subdirectory to save files in. Default is "qc_results"}

\item{design}{Design formula for the DESeq2 object.}
}
\value{
The processed DESeq2 object
}
\description{
Run Sample Distance Pipeline
This function runs the distance analysis portion of the RNA-seq analysis pipeline,
including generating PCA and distance plots, and saving distance data.
}
\examples{
\dontrun{
# Run distance pipeline with default settings
dds <- run_dist_pip(dds)

# Run distance pipeline with custom settings and batch correction
dds <- run_dist_pip(dds, batch = "Batch", save_dir_name = "qc_results")
}
}
