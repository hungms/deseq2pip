% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00_main.R
\name{run_atac_pip}
\alias{run_atac_pip}
\title{Run Complete ATAC-seq Pipeline}
\usage{
run_atac_pip(
  dds,
  org,
  var,
  design,
  batch = NULL,
  comparisons = NULL,
  pals = NULL,
  remove_xy = FALSE,
  remove_mt = FALSE,
  min_count = 10,
  order = "pxfc",
  TSS = TRUE,
  save_dir = getwd()
)
}
\arguments{
\item{dds}{DESeq2 object}

\item{org}{The organism to use, either "human" or "mouse".}

\item{var}{Column name in colData(dds) to use for grouping.}

\item{design}{Design formula for the DESeq2 object.}

\item{comparisons}{Vector of comparisons to run. Default is NULL}

\item{pals}{Vector of colors to use for groups. If NULL, uses default ggplot2 colors. Default is NULL}

\item{remove_xy}{Logical. If TRUE, removes genes on X and Y chromosomes. Default is FALSE}

\item{remove_mt}{Logical. If TRUE, removes mitochondrial genes. Default is FALSE}

\item{min_count}{Minimum count threshold for filtering lowly expressed genes. Default is 10}

\item{order}{Column name to use for ranking genes. Default is "pxfc"}

\item{TSS}{Logical. If TRUE, repeats the analysis for TSS peaks. Default is TRUE}

\item{save_dir}{Directory where all output files will be saved. Default is current working directory}
}
\value{
The processed DESeq2 object
}
\description{
This function runs a complete ATAC-seq analysis pipeline including quality control,
differential expression analysis, and gene set enrichment analysis for all possible
comparisons in the experiment. It generates various plots and saves results in
organized directories.
}
\examples{
\dontrun{
# Run complete pipeline with default settings
dds <- run_atac_pip(dds)

# Run pipeline with custom settings
dds <- run_atac_pip(dds, 
                    org = "mouse", 
                    remove_xy = TRUE,
                    var = "Treatment",
                    save_dir = "output")

}
}
