% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diffexp_functions.R
\name{read_diffexp_list}
\alias{read_diffexp_list}
\title{Read Differential Expression Results}
\usage{
read_diffexp_list(data_dir = getwd(), merge = T)
}
\arguments{
\item{data_dir}{Parent directory containing comparison subdirectories. Default is current working directory}

\item{merge}{Logical. If TRUE, returns a single merged data frame. If FALSE, returns a list of data frames}
}
\value{
Either a merged data frame or a list of data frames containing differential expression results
}
\description{
This function reads differential expression results from multiple comparison directories
and optionally merges them into a single data frame.
}
\examples{
# Read and merge all results
merged_results <- read_diffexp_list(data_dir = "results", merge = TRUE)

# Read results as a list
results_list <- read_diffexp_list(data_dir = "results", merge = FALSE)
}
